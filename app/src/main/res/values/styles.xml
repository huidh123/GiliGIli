<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="windowActionBar">false</item>
    </style>

    <style name="view_sperate_line_style">
        <item name="android:layout_height">1dp</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:background">@color/sperate_bar_color_grey_e4e4e4</item>
    </style>
    
    <style name="textview_label_16sp">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:textSize">@dimen/text_small_size_14sp</item>
        <item name="android:textColor">@color/text_color_grey</item>
    </style>
</resources>
